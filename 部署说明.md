# 数据中心测试验证管理平台 - 内网部署说明

## 一、部署方式概览

| 方式 | 适用场景 | 前置条件 |
|------|----------|----------|
| A. Docker Compose（推荐） | 服务器已安装 Docker | 可拉取 nginx/node/postgres 镜像 |
| B. Nginx + Node + PostgreSQL | 无 Docker 环境 | 手动安装各组件 |

---

## 二、方式 A：Docker Compose 部署

### 1. 上传代码
将整个部署包目录上传到服务器（例如 `/root/test-platform/`）。

### 2. 配置环境变量
```bash
cp .env.example .env
# 编辑 .env，修改 DB_PASSWORD 为强密码
```

### 3. 构建并启动
```bash
docker compose up -d --build
```

### 4. 检查状态
```bash
docker compose ps
# 三个容器应为 Up 状态，postgres 和 backend 为 healthy
```

### 5. 访问
浏览器打开 `http://服务器IP/`

### 镜像源说明
如果内网无法访问公网 Docker Hub，需在 `/etc/docker/daemon.json` 配置内网镜像仓库：
```json
{
  "registry-mirrors": ["https://内网镜像仓库地址"]
}
```
配置后执行 `systemctl restart docker`。

本部署包的 Dockerfile 已使用标准镜像名（`nginx:alpine`、`node:20-slim`），会自动通过 registry-mirror 拉取，无需修改 Dockerfile。

---

## 三、方式 B：Nginx + Node + PostgreSQL 手动部署

### 1. 安装 PostgreSQL 16
```bash
# Ubuntu
apt install -y postgresql
# 创建数据库
sudo -u postgres psql -c "CREATE DATABASE test_platform;"
sudo -u postgres psql -c "ALTER USER postgres PASSWORD '你的密码';"
```

### 2. 部署后端
```bash
cd server
npm install --legacy-peer-deps
npm run build
# 配置环境变量
export DB_HOST=127.0.0.1
export DB_PORT=5432
export DB_NAME=test_platform
export DB_USER=postgres
export DB_PASSWORD=你的密码
export PORT=3001
export NODE_ENV=production
# 启动
node dist/src/index.js
# 建议用 pm2 守护：pm2 start dist/src/index.js --name test-platform-backend
```

### 3. 部署前端
将 `dist/` 目录复制到 nginx 站点目录：
```bash
cp -r dist/* /var/www/test-platform/
```

配置 nginx（参考 `nginx.conf`，核心是把 `/api/` 反代到后端 3001 端口）：
```nginx
server {
    listen 80;
    server_name _;
    root /var/www/test-platform;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 4. 启动 nginx
```bash
nginx -s reload
```

---

## 四、默认账号

| 角色 | 账号 | 密码 |
|------|------|------|
| 管理者（全权限） | admin | admin123 |
| 编辑者（查看+编辑） | editor | editor123 |
| 阅读者（仅查看） | reader | reader123 |

> 首次登录后建议在「权限配置」页面调整账号和权限。

---

## 五、端口说明

| 服务 | 端口 | 暴露范围 |
|------|------|----------|
| 前端（nginx） | 80 | 公网 |
| 后端（Node） | 3001 | 仅本机 |
| 数据库（PostgreSQL） | 5432 | 仅本机 |

---

## 六、数据持久化

- **Docker 方式**：PostgreSQL 数据存储在 docker volume `pg_data`，容器删除后数据保留。
- **手动方式**：数据在 PostgreSQL 默认数据目录。

- **前端业务数据**（项目、人员、历史等）通过 localStorage 持久化在浏览器端，不同浏览器/设备数据独立。

---

## 七、目录结构

```
.
├── dist/                  # 前端构建产物（已构建，可直接部署）
├── public/                # 前端静态资源（含知识库索引 kb/index.json）
├── server/                # 后端源码
│   ├── src/               # TypeScript 源码
│   ├── scripts/           # 资源计算脚本
│   ├── package.json
│   └── tsconfig.json
├── src/                   # 前端源码（Docker 构建时需要）
├── docker-compose.yml     # Docker Compose 编排
├── Dockerfile.frontend    # 前端镜像构建
├── Dockerfile.backend     # 后端镜像构建
├── nginx.conf             # nginx 配置（手动部署参考）
├── package.json           # 前端依赖（Docker 构建时需要）
├── vite.config.ts         # Vite 构建配置
├── .env.example           # 环境变量示例
└── 部署说明.md            # 本文档
```

---

## 八、常见问题

**Q: docker compose build 时拉取镜像超时？**
A: 内网无法访问公网时，需配置内网 Docker 镜像仓库（见第二节"镜像源说明"），或预先在内网镜像仓库导入 `nginx:alpine` 和 `node:20-slim`。

**Q: 后端启动报数据库连接失败？**
A: 检查 .env 中的 DB_HOST/DB_PORT/DB_PASSWORD 是否正确，PostgreSQL 是否运行，5432 端口是否可达。

**Q: 前端页面空白？**
A: 检查 nginx 是否正确指向 dist 目录，`/api/` 是否反代到后端 3001 端口。F12 控制台查看错误。
